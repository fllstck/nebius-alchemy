/**
 * Minimal entry: imports ONLY the storage binding surface — the M0 spike
 * shape (which measured 67 KB / zero gRPC markers).
 */
export {
  GetObject,
  GetObjectHttp,
} from '../modules/resources/storage/v1/bindings.ts'
